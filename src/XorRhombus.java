import java.util.List;

public class XorRhombus extends XOORhombus{

    public XorRhombus(Integer kantenIndex, List<Object> list, List<Kante> kantenList, Object refStart, Object refEnd, Parameters parameters) {
        super(kantenIndex, list, kantenList, refStart, refEnd, parameters);
    }
}
